<nav class="fixed top-0 left-0 right-0 z-50 bg-black/80 backdrop-blur-md border-b border-[#00FF00]/20">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center h-16">
      <a href="/" class="text-xl font-bold text-[#00FF00] hover:text-[#00FF00] transition-colors flex items-center">
        <span class="terminal-text text-[#00FF00]">></span>
        <span class="ml-2 glow-text text-[#00FF00]">ANDRÉS FABREGAS</span>
      </a>
      
      <div class="hidden md:flex space-x-8">
        <a href="#about" class="nav-link text-[#00FF00]">Sobre Mí</a>
        <a href="#skills" class="nav-link text-[#00FF00]">Habilidades</a>
        <a href="#projects" class="nav-link text-[#00FF00]">Proyectos</a>
        <a href="#contact" class="nav-link text-[#00FF00]">Contacto</a>
      </div>
      
      <button id="mobile-menu-button" class="md:hidden text-[#00FF00] hover:text-[#00FF00] transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>
  
  <!-- Mobile menu -->
  <div id="mobile-menu" class="md:hidden hidden bg-black/90 border-t border-[#00FF00]/20">
    <div class="container mx-auto px-4 py-4 space-y-4">
      <a href="#about" class="block nav-link text-[#00FF00]">Sobre Mí</a>
      <a href="#skills" class="block nav-link text-[#00FF00]">Habilidades</a>
      <a href="#projects" class="block nav-link text-[#00FF00]">Proyectos</a>
      <a href="#contact" class="block nav-link text-[#00FF00]">Contacto</a>
    </div>
  </div>
</nav>

<style>
  .nav-link {
    @apply text-[#00FF00] hover:text-[#00FF00] transition-colors relative py-2;
  }
  
  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, #00FF00, transparent);
    transition: width 0.3s ease;
  }
  
  .nav-link:hover::after {
    width: 100%;
  }
  
  .glow-text {
    text-shadow: 0 0 5px rgba(0, 255, 0, 0.5), 0 0 10px rgba(0, 255, 0, 0.3);
  }
  
  .terminal-text {
    font-family: 'Courier New', Courier, monospace;
    animation: blink 1s step-end infinite;
  }
  
  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }
</style>

<script>
  document.getElementById('mobile-menu-button')?.addEventListener('click', () => {
    const mobileMenu = document.getElementById('mobile-menu');
    if (mobileMenu) {
      mobileMenu.classList.toggle('hidden');
    }
  });
</script> 